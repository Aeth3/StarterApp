/**
 * Session entity shape.
 * @typedef {Object} Session
 * @property {string|{token:string}} access_token
 * @property {string} [refresh_token]
 * @property {Object} [user]
 */
export const SessionEntity = {};
