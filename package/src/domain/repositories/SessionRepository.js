/**
 * SessionRepository interface (documentation-only).
 * Implementations should provide these async methods:
 * - saveSession(session)
 * - getSession()
 * - clearSession()
 * - getAccessToken()
 */
export class SessionRepository {}
