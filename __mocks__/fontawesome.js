const React = require("react");
const { View } = require("react-native");

const FontAwesomeIcon = (props) => React.createElement(View, props);

module.exports = {
  FontAwesomeIcon,
  default: FontAwesomeIcon,
};
